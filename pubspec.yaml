name: passcontainer
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  module_navigator:
    git:
      url: git@github.com:yago1404/module_navigator.git
      ref: develop
  cupertino_icons: ^1.0.6
  http: ^1.2.1
  hive: ^2.2.3
  path_provider: ^2.1.3
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

  assets:
    - .env